# 酒店数据分析系统 — 迁移操作手册

> 适用版本：v1.0 | 更新：2026-07-22

---

## 目录

1. [迁移前准备](#1-迁移前准备)
2. [数据完整性检查](#2-数据完整性检查)
3. [打包传输](#3-打包传输)
4. [新设备部署](#4-新设备部署)
5. [首次启动验证](#5-首次启动验证)
6. [登录与确认](#6-登录与确认)
7. [常见问题处理](#7-常见问题处理)
8. [日常更新流程](#8-日常更新流程)

---

## 1. 迁移前准备

### 1.1 确认当前系统正常

在旧设备上，确保：
- [ ] 系统能正常启动，浏览器访问 `http://127.0.0.1:8080` 可打开
- [ ] 最近一次自动采集成功
- [ ] 数据页面能正常显示

### 1.2 手动备份一次

在系统设置页面 →「数据维护」→「📦 立即备份」，或直接拷贝：

```
旧设备：D:\SUJI\前台\hotel-analysis\data\hotel_data.db
```

> ⚠️ 备份时请先关闭系统（托盘右键 → 退出），否则 SQLite 可能正在写入。

### 1.3 确认文件夹结构

迁移需要**整个项目文件夹**，结构如下：

```
hotel-analysis\
├── main.py                 ← 程序入口
├── requirements.txt        ← Python 依赖清单
├── build.spec              ← 打包配置
├── app\                    ← 所有 Python 后端代码
├── frontend\               ← Vue 前端源码（含 node_modules）
├── dist\                   ← 前端构建产物（必须！）
├── data\                   ← ⭐ 核心数据目录
│   ├── hotel_data.db       ← 数据库（最重要！）
│   ├── config.ini          ← 系统配置
│   ├── backup\             ← 自动备份
│   ├── export\             ← 导出留存
│   ├── import\             ← 导入临时文件
│   └── logs\               ← 运行日志
└── config.default.ini      ← 默认配置模板
```

### 1.4 记录关键信息

迁移前记下这些信息，方便在新设备上确认：

| 信息 | 当前值 | 获取方式 |
|------|--------|---------|
| 总房间数 | 113 | 系统常量 |
| 服务端口 | `data/config.ini` 中 port 值 | 系统设置页查看 |
| 数据采集日期范围 | __年__月__日 至 __年__月__日 | 日报数据页查看 |
| 用户列表 | ______________ | 系统设置→用户管理 |
| 最近备份时间 | ______________ | data/backup/ 目录 |

---

## 2. 数据完整性检查

### 2.1 检查数据库

在旧设备命令行执行：

```powershell
cd D:\SUJI\前台\hotel-analysis
python -c "import sqlite3; c=sqlite3.connect('data/hotel_data.db').cursor(); c.execute('PRAGMA integrity_check'); print(c.fetchone()[0])"
```

应输出 `ok`。如果不是，说明数据库已损坏，需要从备份恢复。

### 2.2 检查数据量

```powershell
python -c "import sqlite3; c=sqlite3.connect('data/hotel_data.db').cursor(); c.execute('SELECT COUNT(*) FROM daily_data'); print(f'日报: {c.fetchone()[0]} 条'); c.execute('SELECT COUNT(*) FROM hourly_data'); print(f'小时: {c.fetchone()[0]} 条')"
```

记录这两个数字，迁移后对照。

### 2.3 检查备份

```
data\backup\ 目录下应有多个 hotel_data_YYYYMMDD_HHMM.db 文件。
最近一次备份不应超过 6 小时。
```

---

## 3. 打包传输

### 3.1 压缩整个文件夹

**方法一（推荐）**：
1. 关闭系统（托盘右键 → 退出）
2. 右键 `hotel-analysis` 文件夹 →「发送到」→「压缩(zipped)文件夹」
3. 得到 `hotel-analysis.zip`

**方法二（命令行）**：
```powershell
cd D:\SUJI\前台
Compress-Archive -Path ".\hotel-analysis" -DestinationPath ".\hotel-analysis-迁移.zip"
```

### 3.2 传输到新设备

- U 盘拷贝
- 局域网共享
- 微信/钉钉文件传输（注意：压缩包可能较大，含 node_modules 约 200MB）

> ⚠️ 务必传输**压缩包**，不要直接拷贝零散文件，否则可能丢失或损坏。

---

## 4. 新设备部署

### 4.1 环境要求

| 要求 | 说明 |
|------|------|
| 操作系统 | Windows 10 或 Windows 11（64 位） |
| Python | 3.11 及以上 |
| 磁盘空间 | 至少 500MB 可用 |
| 浏览器 | Chrome / Edge / Firefox 均可 |

### 4.2 检查 Python

```powershell
python --version
```

应显示 `Python 3.11.x` 或更高。如果没有，访问 https://www.python.org/downloads/ 下载安装。

> ⚠️ 安装 Python 时务必勾选「Add Python to PATH」

### 4.3 解压文件

1. 将 `hotel-analysis-迁移.zip` 解压到**英文路径**下，例如：
   ```
   D:\hotel-analysis\
   C:\Users\你的用户名\Desktop\hotel-analysis\
   ```
   > ❌ 不要放在包含中文、空格、特殊符号的路径下

2. 解压后的文件夹结构应和迁移前一致

### 4.4 安装 Python 依赖

```powershell
cd D:\hotel-analysis    # 进入解压后的目录
pip install -r requirements.txt
```

如果提示 `pip` 不是内部命令：
```powershell
python -m pip install -r requirements.txt
```

### 4.5 初始化数据库

首次启动会自动创建数据库和默认配置。也可以手动触发：

```powershell
python -c "from app.db.database import init_database; init_database(); print('数据库就绪')"
```

> 如果 `data\hotel_data.db` 已存在（从旧设备迁移过来的），系统会自动识别并只执行缺失的迁移。

### 4.6 首次启动

```powershell
python main.py
```

正常输出：
```
目录初始化完成
数据库初始化完成
[DB] 迁移 v1: xxx (已存在，跳过)
定时任务启动完成
系统托盘启动完成
服务启动成功，访问地址：http://127.0.0.1:8080
```

浏览器会自动打开。若没有，手动访问 `http://127.0.0.1:8080`。

---

## 5. 首次启动验证

### 5.1 检查首页

浏览器打开后，应看到：
- 左侧深蓝色菜单栏
- 右侧「实时仪表盘」页面
- 四个指标卡片（出租率、已售房间、RevPar、ADR）
- 小时趋势图表

### 5.2 检查数据

进入「日报数据」页面，选择一个日期范围（如最近 30 天），确认：
- [ ] 表格有数据
- [ ] 图表正常渲染
- [ ] 出租率、RevPar、ADR 数值合理

### 5.3 检查自动采集

1. 顶栏点击「自动采集」
2. 确认弹窗 → 点击「开始采集」
3. 等待几秒，提示"采集完成"
4. 进入「小时数据」页，确认有新的小时数据

### 5.4 检查定时任务

进入「系统设置」→ 查看系统状态，确认定时任务正在运行。

---

## 6. 登录与确认

### 6.1 默认账号

| 用户名 | 密码 | 角色 | 用途 |
|--------|------|------|------|
| 管理员 | 123456 | 管理员 | **日常使用**，可编辑数据、导入导出、系统设置 |
| 超管 | 123456 | 超级管理员 | 用户管理、配置保护项修改 |
| 前台 | — | 操作员 | 仅查看数据、手动采集 |

> 🔐 首次登录后请尽快修改默认密码！

### 6.2 登录操作

1. 点击右上角用户标签（显示"未登录🔓"）
2. 选择「管理员」
3. 输入 PIN：`123456`
4. 确认

### 6.3 修改密码

1. 点击右上角 →「修改密码」
2. 输入旧密码 `123456`
3. 输入新密码（6 位数字）
4. 确认

---

## 7. 常见问题处理

### Q1：启动时提示"程序已经在运行"

```powershell
del D:\hotel-analysis\data\.lock
```
然后重新启动。如果还不行，打开任务管理器结束所有 `python.exe` 进程。

### Q2：端口 8080 被占用

系统会自动尝试 8081、8082……最多 10 次。如果全部被占：
```powershell
netstat -ano | findstr ":808"    # 查看占用端口的进程
```

### Q3：浏览器显示 `{"detail":"Not Found"}`

前端文件未正确加载。执行：
```powershell
cd D:\hotel-analysis\frontend
npm install
npm run build
cd ..
python main.py
```

### Q4：自动采集失败

1. 进入「系统设置」→「Cookie 管理」
2. 点击「📋 一键读取 Cookie」
3. 在打开的浏览器中登录去呼呼
4. Cookie 自动保存后重新采集

### Q5：数据对不上

1. 检查「小时数据」页面 → 选择对应日期
2. 看 24:00 那行是否是最终数据
3. 或者手动点击「一键记录」重新采集

### Q6：数据库损坏

```powershell
# 停止系统
# 从备份恢复：
copy data\backup\hotel_data_YYYYMMDD_HHMM.db data\hotel_data.db
# 重新启动
python main.py
```

### Q7：换了一台新电脑，原来的电脑还要用

**不支持两台电脑同时使用同一个数据库**。如果要在两台电脑上用，需要各装一份，各自维护数据。

---

## 8. 日常更新流程

### 8.1 代码更新（不改数据结构）

1. 关闭系统
2. 替换 `app\` 文件夹中的所有 `.py` 文件
3. 替换 `dist\` 文件夹
4. 重启系统

### 8.2 有数据库迁移的更新

1. 关闭系统
2. 替换全部代码文件
3. 重启系统 → 自动执行新迁移
4. 验证数据正常

### 8.3 回滚

如果更新后出现问题：
1. 关闭系统
2. 从 `data\backup\` 恢复更新前的数据库
3. 换回旧代码
4. 重启

---

## 附录：关键路径速查

| 路径 | 说明 |
|------|------|
| `data\hotel_data.db` | ⭐ 数据库，最重要 |
| `data\config.ini` | 系统配置（端口、采集间隔等） |
| `data\backup\` | 自动备份的数据库副本 |
| `data\logs\app.log` | 运行日志，排查问题时查看 |
| `data\.lock` | 单实例锁文件，异常退出时手动删除 |
| `dist\` | 前端页面，修改 UI 后需要 `npm run build` |

---

> 📝 迁移成功的关键：**完整拷贝文件夹 → 安装 Python 依赖 → 保证 data/hotel_data.db 完好**。做到这三点，迁移就不可能有任何问题。
