# 酒店数据分析系统 — 技术&操作手册

> 版本：v1.0 | 更新：2026-07-22 | 适用：苏季酒店（去呼呼PMS）

---

## 目录

1. [系统概述](#1-系统概述)
2. [技术架构](#2-技术架构)
3. [安装与部署](#3-安装与部署)
4. [启动与访问](#4-启动与访问)
5. [用户角色与权限](#5-用户角色与权限)
6. [界面功能指南](#6-界面功能指南)
   - 6.1 [实时仪表盘](#61-实时仪表盘)
   - 6.2 [小时数据](#62-小时数据)
   - 6.3 [日报数据](#63-日报数据)
   - 6.4 [月报数据](#64-月报数据)
   - 6.5 [季报数据](#65-季报数据)
   - 6.6 [年报数据](#66-年报数据)
   - 6.7 [图表分析](#67-图表分析)
   - 6.8 [热点日历](#68-热点日历)
   - 6.9 [系统设置](#69-系统设置)
7. [自动采集机制](#7-自动采集机制)
8. [数据导入导出](#8-数据导入导出)
9. [订单流速表](#9-订单流速表)
10. [备份与还原](#10-备份与还原)
11. [计算公式](#11-计算公式)
12. [数据库结构](#12-数据库结构)
13. [常见问题排查](#13-常见问题排查)

---

## 1. 系统概述

### 什么是本系统？
纯本地运行的酒店运营数据分析系统。对接**去呼呼客栈管家PMS**，实现：
- 自动定时采集客房销售数据
- 五级数据聚合（小时→日→月→季→年）
- 多维度图表分析与价格预测
- 订单流速表（Excel专业报表）
- 用户权限管理（超管/管理员/操作员）

### 关键指标
| 指标 | 英文 | 公式 |
|------|------|------|
| 出租率 | Occupancy | 已售÷总房间数×100% |
| 单房收益 | RevPar | 总房费÷总房间数 |
| 平均房价 | ADR | 总房费÷已售房间数 |

### 技术特点
- **零依赖绿色版**：单文件夹即拷即用，无需安装数据库/运行环境
- **纯本地运行**：SQLite数据库本地存储，不上传云端
- **系统托盘**：后台运行不占任务栏，托盘图标管理
- **单实例锁**：防止重复启动

---

## 2. 技术架构

```
┌──────────────────────────────────────────────────┐
│                    浏览器                         │
│              http://127.0.0.1:8080               │
└─────────────────┬────────────────────────────────┘
                  │
┌─────────────────▼────────────────────────────────┐
│              FastAPI (Python 3.11)                │
│  ┌──────────┬──────────┬──────────┬────────────┐ │
│  │ 系统API  │ 数据API  │ 导入导出 │  图表API   │ │
│  └──────────┴──────────┴──────────┴────────────┘ │
│  ┌──────────────────────────────────────────────┐ │
│  │           核心引擎                            │ │
│  │  计算器 │ 采集器 │ 清洗器 │ 调度器 │ 流速表  │ │
│  └──────────────────────────────────────────────┘ │
│  ┌──────────────────────────────────────────────┐ │
│  │         SQLite (WAL模式)                      │ │
│  │   hotel_data.db (单文件)                      │ │
│  └──────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────┘
```

### 技术栈
| 层 | 技术 | 说明 |
|----|------|------|
| 后端框架 | FastAPI + uvicorn | 端口 8080，仅监听 127.0.0.1 |
| 数据库 | SQLite (WAL模式) | `data/hotel_data.db` |
| 前端 | Vue 3 + Element Plus + ECharts | Vite 打包为静态文件 |
| 定时任务 | APScheduler | 北京时间 (Asia/Shanghai) |
| 采集引擎 | 去呼呼API + Playwright | Cookie 自动登录 |
| Excel | openpyxl | 订单流速表生成 |
| 打包 | PyInstaller | 绿色单文件 EXE |

### 5 级数据层级
```
hourly_data (小时) → daily_data (日) → monthly_data (月) → quarterly_data (季) → yearly_data (年)
```

数据流：API采集写入 `hourly_data` → `daily_aggregate` 汇总到 `daily_data` → 月度/季度/年度聚合逐级汇总

---

## 3. 安装与部署

### 3.1 绿色版（推荐）

1. 获取 `酒店数据分析系统.zip`
2. 解压到任意文件夹（如 `D:\hotel-analysis\`）
3. 双击 `酒店数据分析系统.exe`
4. 浏览器自动打开 `http://127.0.0.1:8080`

### 3.2 源码运行（开发调试）

**环境要求**：Python 3.11+、Node.js 18+

```bash
# 1. 克隆或解压项目
cd hotel-analysis

# 2. 安装 Python 依赖
pip install -r requirements.txt

# 3. 安装 Playwright 浏览器（Cookie 采集用）
playwright install chromium

# 4. 启动后端
python main.py

# 5. 构建前端（首次或修改前端后）
cd frontend
npm install
npm run build
cd ..
```

### 3.3 目录结构

```
hotel-analysis/
├── main.py              # 程序入口
├── app/                 # Python 后端
│   ├── api/             # API 路由
│   ├── core/            # 核心引擎
│   ├── db/              # 数据库
│   └── utils/           # 工具
├── frontend/            # Vue 前端源码
├── dist/                # 前端构建产物
├── data/                # 用户数据（数据库/备份/日志）
│   ├── hotel_data.db
│   ├── config.ini
│   ├── backup/
│   ├── export/
│   ├── import/
│   └── logs/
└── requirements.txt
```

---

## 4. 启动与访问

### 启动
- **绿色版**：双击 EXE
- **源码**：`python main.py`

### 访问
浏览器自动打开 `http://127.0.0.1:8080`

### 任务栏托盘
程序启动后在系统托盘区显示图标：
- **右键**：显示/隐藏窗口、打开浏览器、退出程序
- 退出时自动备份数据库

### 端口配置
默认端口 **8080**。如需修改：
- 进入「系统设置」页面修改端口
- 或编辑 `data/config.ini` 中 `port` 值
- 重启生效

---

## 5. 用户角色与权限

系统内置三级角色，通过 6 位数字 PIN 码验证身份。

### 角色权限表

| 功能 | 超管 | 管理员 | 操作员 | 游客 |
|------|:---:|:-----:|:-----:|:---:|
| 查看所有数据 | ✅ | ✅ | ✅ | ✅ |
| 查看图表分析 | ✅ | ✅ | ✅(仅日/周/月) | ❌ |
| 手动采集数据 | ✅ | ✅ | ✅ | ❌ |
| 编辑/删除数据 | ✅ | ✅ | ❌ | ❌ |
| 导入/导出数据 | ✅ | ✅ | ❌ | ❌ |
| 批量操作 | ✅ | ✅ | ❌ | ❌ |
| 系统设置 | ✅ | ✅ | ❌ | ❌ |
| 配置保护项 | ✅ | ❌ | ❌ | ❌ |
| 用户管理 | ✅ | ❌ | ❌ | ❌ |
| 退出登录 | 返回游客 | 返回游客 | 返回游客 | — |

### 默认账号

| 用户名 | 默认PIN | 角色 | 说明 |
|--------|---------|------|------|
| 超管 | 123456 | super_admin | 不可删除/禁用/降级 |
| 管理员 | 123456 | admin | 不可删除/禁用/降级（需手动创建） |

### 操作

**登录**：点击右上角用户标签 → 选择用户 → 输入 PIN → 确认

**修改密码**：点击右上角 →「修改密码」→ 输入旧PIN → 输入新PIN

**切换用户**：点击右上角 → 选择其他用户 → 输入 PIN

**退出登录**：点击右上角 →「退出登录」→ 返回游客状态

### 用户管理（仅超管）

「系统设置」→「用户管理」标签：
- **创建**：填写用户名、PIN、角色
- **编辑**：修改用户名/PIN/角色
- **启用/禁用**：禁用后无法登录
- **删除**：受保护用户不可删除

---

## 6. 界面功能指南

### 6.1 实时仪表盘

**路径**：左侧菜单 →「实时仪表盘」

**功能**：
- **4 个指标卡**：当日出租率、已售房间、单房收益(RevPar)、平均房价(ADR)
- **小时趋势图**：当日 1:00-24:00 累计房费（柱状）、单房收益（折线）、平均房价（虚线）、出租率（折线）

**数据来源**：当天 `hourly_data` 表，取最新有数据的小时

---

### 6.2 小时数据

**路径**：左侧菜单 →「小时数据」

**表格列**：时间、已售、可售、出租率、起售价格、单房收益、平均房价、累计房费

**操作**：

| 按钮 | 权限 | 说明 |
|------|:---:|------|
| 📤 模板 | 管理员 | 下载当前日期 24 小时 CSV 模板（已有数据预填） |
| 📥 导入 | 管理员 | 上传 CSV 批量导入（列：data_date,data_hour,sold_rooms,total_revenue,min_price） |
| 批量删除 | 管理员 | 选中多行删除（h24 不可选） |
| 🔄 回填h24 | 管理员 | 用日报数据补全缺失的 24 点数据（历史日期才显示） |
| **一键记录** | 全部 | 从去呼呼 API 采集当前日期数据并入库 |
| 日期选择器 | — | 切换查看日期 |

**美团底价更新**：
- 输入美团当天最低价 →「一键更新当天所有小时起售价格」
- 更新后自动重算日报和月报

**图表**：累计房费柱状图 + 单房收益折线 + 平均房价虚线

---

### 6.3 日报数据

**路径**：左侧菜单 →「日报数据」

**表格列**：日期、起售价格、售出房间、剩余房间、出租率、单房收益、平均房价、累计房费

**操作**：

| 按钮 | 权限 | 说明 |
|------|:---:|------|
| 📤 模板 | 管理员 | 下载日报 CSV 模板 |
| 导出 | 管理员 | 导出当前日期范围的 CSV |
| 批量删除 | 管理员 | 选中多日报数据删除 |

**图表**：累计房费柱状图 + 单房收益折线 + 平均房价虚线（含缩放滑块）

---

### 6.4 月报数据

**路径**：左侧菜单 →「月报数据」

**表格列**：周期（年月）、天数、已售房间数、出租率、单房收益、平均房价、累计房费

**交互**：点击某月 → 显示该月每日明细图表（累计房费柱状 + 单房收益折线 + 平均房价虚线）

---

### 6.5 季报数据

**路径**：左侧菜单 →「季报数据」

**表格列**：周期（年+Q）、天数、已售房间数、平均出租率、平均单房收益、平均房价、累计房费

---

### 6.6 年报数据

**路径**：左侧菜单 →「年报数据」

**表格列**：年份、有效天数、累计售出房间、平均出租率、平均单房收益、平均房价、累计房费

---

### 6.7 图表分析

**路径**：左侧菜单 →「图表分析」

#### 三个分析标签

**1. 趋势分析**
- **维度**：日 / 周 / 月 / 季度 / 年（操作员仅日/周/月）
- **可选指标**：
  - 预售占比（出租率）
  - 起售价格
  - 单房收益
  - 平均房价
  - 售出房间
  - 累计房费
  - 预测起售价格（周/月维度，Holt-Winters 算法）
- **图表**：柱状+折线多系列组合图，支持 dataZoom

**2. 热度矩阵**
- **日**：选中日期±3天×24小时
- **周**：周一到周日日售房间数
- **月/季度/年**：支持切换「热度矩阵」和「星期分布」视图

**3. 定价象限**（仅管理员）
- 最近 90 天价格 vs 入住率散点图
- 辅助判断价格合理性

---

### 6.8 热点日历

**路径**：左侧菜单 →「热点日历」

**功能**：
- 月视图/年视图切换
- 显示节假日、天气信息
- 点击「一键更新」同步最新天气数据
- 数据源：和风天气 + open-meteo

---

### 6.9 系统设置

**路径**：左侧菜单 →「系统设置」（仅管理员）

#### 系统配置标签
| 配置项 | 说明 | 默认值 |
|--------|------|--------|
| collection_interval | 自动采集间隔（分钟） | 60 |
| dev_mode | 开发模式开关（显示对账） | 0 |
| port | 服务端口 | 8080 |
| auto_backup_hours | 自动备份间隔（小时） | 6 |
| meituan_enabled | 美团房价采集开关 | 0 |
| meituan_username | 美团商家手机号 | — |
| meituan_password | 美团商家密码 | — |

> ⚠️ `dev_mode`、`port`、`collection_interval` 为受保护配置，仅超管可修改

#### Cookie 管理标签
- **去呼呼 Cookie**：一键读取 → 打开登录页 → 扫码 → 自动保存 → 测试
- **美团 Cookie**（可选）：同上

#### 用户管理标签（仅超管）
- 用户列表、创建、编辑、禁用、删除

#### 数据维护标签
- **全量重算**：从日报重新生成所有月报/季报/年报
- **指定日期重算**：重算单日 → 级联更新月报
- **指定月份重算**：重算单月报

#### 操作日志标签
- 最近 50 条审计日志（谁在什么时间做了什么）

---

## 7. 自动采集机制

### 定时采集
APScheduler 每小时触发一次：

| 时间 | 采集目标 | 存入小时 |
|------|---------|---------|
| 00:02 | 昨天 | h24（日终结算） |
| 01:02 | 今天 | h1 |
| 02:02 | 今天 | h2 |
| ... | ... | ... |
| 23:02 | 今天 | h23 |

> 执行分钟偏移量由 `collect_offset_minute` 配置（默认 2 分钟）

### 手动采集

**方式一**：任意页面顶栏「自动采集」按钮 → 采集今日数据

**方式二**：小时数据页「一键记录」按钮 → 采集选中日期数据

### 凌晨特殊处理
凌晨 0:00-0:59 手动采集时：
- 若选择"今天"→ 自动切换为采集**昨天 h24**
- 若选择历史日期 → 采集该日期 h24

### 采集流程
```
去呼呼API直连 → 导出Excel → 解析清洗 → calc_hourly计算 → 写入hourly_data
→ daily_aggregate → monthly_aggregate
```

### Cookie 管理
- Cookie 保存在数据库 `sys_config` 表中
- Cookie 过期时手动进入「系统设置」→「Cookie管理」→「一键读取」重新获取
- 采集前自动检测 Cookie 有效性

---

## 8. 数据导入导出

### CSV 导入

**小时数据导入**：
1. 小时数据页 →「📤 模板」下载当前日期模板
2. 编辑 CSV（列：data_date, data_hour, sold_rooms, total_revenue, min_price）
3.「📥 导入」上传 CSV → 自动计算所有指标 → 写入并重算日报/月报

**日报数据导入**：
1. 日报数据页 →「📤 模板」下载模板
2. 编辑 CSV（列：data_date, sold_rooms, total_revenue, min_price）
3.「📥 导入」上传 CSV

**起售价格批量更新**：
- 下载起售价格模板 → 填入日期和价格 → CSV 批量更新

### Excel 导入（历史数据）
「系统设置」→「导入历史Excel」→ 上传去呼呼月度导出的 Excel 文件

### 导出
- 日报数据页 →「导出」→ 下载当前日期范围的 CSV

---

## 9. 订单流速表

**路径**：顶栏「订单流速表」按钮

**功能**：生成专业 Excel 报表，展示最近 7 天逐小时流速数据。

### Excel 表格结构

```
┌──────┬──────────────────────────────────────────────┐
│      订单流速表                                      │
│      房间数量：113                                   │
├──────┬──────────┬──────────┬──────────┬─────────────┤
│      │ 07月20日 周一        │ 07月21日 周二         │
│ 时间  │ 可售/已售│ ADR│RevPar│ 可售/已售│ ADR│RevPar│
├──────┼──────────┼─────┼──────┼──────────┼─────┼─────┤
│01:00 │ 113/0    │     │      │ ...                   │
│...   │ ...      │ ... │ ...  │ ...                   │
│24:00 │ 87/26    │ 245 │  54  │ 89/24    │ 238 │ 51  │ (淡黄高亮)
└──────┴──────────┴─────┴──────┴──────────┴─────┴─────┘
```

### 说明
- 每列颜色按天交替（极淡蓝/白）
- 24:00 行淡黄高亮 = 当日结算
- ADR（平均房价）= 累计房费÷已售房间数
- RevPar（单房收益）= 累计房费÷113
- 数字格式：整数不显示 `.00`，小数显示 2 位
- 表头冻结，向下滚动不丢标题

---

## 10. 备份与还原

### 自动备份
- 每 6 小时自动备份（02:00、08:00、14:00、20:00）
- 启动时自动备份一次
- 备份文件：`data/backup/hotel_data_YYYYMMDD_HHMMSS.db`

### 手动备份
「系统设置」→「数据维护」→「手动备份」

### 还原
「系统设置」→「数据维护」→ 选择备份文件 →「还原」
> ⚠️ 还原会覆盖当前数据库，建议先手动备份

---

## 11. 计算公式

所有公式统一定义在 `app/core/calculator.py`，精度 2 位小数。

### 小时级 (calc_hourly)
```
available_rooms = 113 - sold_rooms
occupancy_rate  = sold_rooms / 113 × 100
revpar          = total_revenue / 113
adr             = total_revenue / sold_rooms  (sold_rooms > 0)
```

### 日级 (calc_daily)
```
remain_rooms    = 113 - sold_rooms
occupancy_rate  = sold_rooms / 113 × 100
revpar          = total_revenue / 113
adr             = total_revenue / sold_rooms  (sold_rooms > 0)
```

### 月级 (calc_monthly)
```
occupancy_rate  = total_sold / (113 × valid_days) × 100
revpar          = total_revenue / (113 × valid_days)
adr             = total_revenue / total_sold  (total_sold > 0)
```

### 季级 (calc_quarterly)
```
occupancy_rate  = total_sold / (113 × valid_days) × 100
revpar          = total_revenue / (113 × valid_days)
adr             = total_revenue / total_sold
```

### 年级 (calc_yearly)
```
occupancy_rate  = total_sold / (113 × valid_days) × 100
revpar          = total_revenue / (113 × valid_days)
adr             = total_revenue / total_sold
```

---

## 12. 数据库结构

### 数据表

| 表名 | 说明 | 主键/唯一 |
|------|------|----------|
| hourly_data | 小时数据 | UNIQUE(data_date, data_hour) |
| daily_data | 日报数据 | UNIQUE(data_date) |
| monthly_data | 月报数据 | UNIQUE(data_year, data_month) |
| quarterly_data | 季报数据 | UNIQUE(data_year, data_quarter) |
| yearly_data | 年报数据 | UNIQUE(data_year) |
| sys_config | 系统配置 | UNIQUE(config_key) |
| users | 用户表 | UNIQUE(name) |
| import_record | 导入记录 | — |
| operation_log | 操作日志 | — |
| audit_log | 审计日志 | — |

### 各数据表关键字段

```
hourly_data:
  data_date, data_hour (1-24), sold_rooms, available_rooms,
  occupancy_rate, min_price, revpar, total_revenue, adr, data_source

daily_data:
  data_date, min_price, sold_rooms, remain_rooms,
  occupancy_rate, revpar, total_revenue, adr, data_source

monthly_data:
  data_year, data_month, days, sold_rooms,
  occupancy_rate, revpar, total_revenue, adr

quarterly_data:
  data_year, data_quarter, days, sold_rooms,
  occupancy_rate, revpar, total_revenue, adr

yearly_data:
  data_year, valid_days, sold_rooms,
  occupancy_rate, revpar, total_revenue, adr
```

### data_source 含义

| 值 | 含义 | 说明 |
|----|------|------|
| 1 | AUTO_IMPORT | 自动采集 |
| 2 | MANUAL_INPUT | 手动导入 |
| 3 | MANUAL_EDIT | 手动修改 |

> 自动采集不会覆盖 `data_source ≥ 2` 的数据（保护手动数据）

---

## 13. 常见问题排查

### Q1：自动采集失败
1. 进入「系统设置」→「Cookie管理」→「一键读取」重新获取 Cookie
2. 确保去呼呼账号未过期
3. 检查 `data/logs/` 日志查看具体错误

### Q2：数据不显示或显示 0
1. 确认日期选择正确
2. 检查是否有对应日期的小时数据（先采集数据）
3. 尝试「数据维护」→「全量重算」

### Q3：ADR 显示为 0
1. 检查 `sold_rooms` 是否为 0（分母为零则 ADR=0）
2. 重启应用后该列自动补充（新代码已修复所有 INSERT 缺 ADR 问题）

### Q4：凌晨采集变成当天 h24
- **已修复**：凌晨 0:00-0:59 手动采集自动切换为昨天 h24
- **需重启**：代码修改后必须重启应用生效

### Q5：前端不更新
- 修改 `.vue` 文件后需要 `cd frontend && npm run build`
- 构建后重启 Python 应用

### Q6：端口被占用
- 默认 8080，修改 `data/config.ini` 中 `port` 值
- 或命令行：`python main.py --port 8081`

### Q7：数据库损坏
- 从 `data/backup/` 恢复最近的备份
- 「系统设置」→「数据维护」→「还原」

### Q8：如何修改总房间数
- 编辑 `app/constants.py` 中 `TOTAL_ROOMS = 113`
- 修改后重启应用
- 已有数据需要「全量重算」

### Q9：Python 进程不退出
- 任务管理器结束 `python.exe` 进程
- 或右键托盘图标 →「退出」

### Q10：Cookie 过期
- 去呼呼 Cookie 有效期约 24 小时
- 系统设置中可测试 Cookie 有效性
- 过期后重新「一键读取」

---

## 附录 A：API 接口速查

### 系统接口 (system_api)
| 方法 | 路径 | 功能 |
|------|------|------|
| GET | /config/list | 获取所有配置 |
| PUT | /config | 更新配置 |
| POST | /role/switch | 用户登录 |
| POST | /role/logout | 退出登录 |
| PUT | /role/change-pin | 修改密码 |
| GET | /user/list | 用户列表 |
| POST | /user/create | 创建用户 |
| PUT | /user/update | 更新用户 |
| DELETE | /user/{id} | 删除用户 |
| GET | /backup/list | 备份列表 |
| POST | /backup/create | 创建备份 |
| POST | /backup/restore | 还原备份 |
| POST | /cookie/capture | 采集 Cookie |
| POST | /daily/recalculate | 重算指定日报 |
| POST | /recalculate/all | 全量重算 |

### 数据接口 (data_api)
| 方法 | 路径 | 功能 |
|------|------|------|
| GET | /hourly/list | 小时数据列表 |
| POST | /hourly/update | 更新小时数据 |
| POST | /hourly/batch-import | CSV 导入小时 |
| POST | /hourly/backfill-h24 | 回填 h24 |
| PUT | /hourly/update-min-price | 批量更新起售价 |
| DELETE | /hourly/{id} | 删除小时数据 |
| DELETE | /hourly/date/{date} | 删全天小时 |
| GET | /daily/list | 日报列表 |
| POST | /daily/update | 更新日报 |
| POST | /daily/batch-import | CSV 导入日报 |
| POST | /daily/batch-delete | 批量删除日报 |
| GET | /monthly/list | 月报列表 |
| GET | /monthly/daily-detail | 月度每日明细 |
| GET | /quarterly/list | 季报列表 |
| GET | /yearly/list | 年报列表 |

### 导入导出 (import_export_api)
| 方法 | 路径 | 功能 |
|------|------|------|
| POST | /import/excel | 导入 Excel 报表 |
| POST | /import/auto-collect | 单日自动采集 |
| POST | /import/auto-collect-history | 批量历史采集 |
| GET | /export/order-flow | 导出订单流速表 |
| GET | /export/excel | 导出数据 Excel |

### 图表接口 (chart_api)
| 方法 | 路径 | 功能 |
|------|------|------|
| GET | /chart/hourly/trend | 小时趋势 |
| GET | /chart/daily/trend | 日趋势 |
| GET | /chart/heatmap | 热力图 |
| GET | /chart/weekday | 星期分布 |
| GET | /chart/price-scatter | 定价散点 |

---

## 附录 B：定时任务一览

| 任务 | 触发时间 | 说明 |
|------|---------|------|
| 自动采集 | 每小时（偏移2分） | 去呼呼 API 采集 |
| 日报聚合 | 每天 00:01 | h24→日报 |
| 月报聚合 | 每月1日 00:01 | 日报→月报 |
| 季报聚合 | 1/4/7/10月1日 00:02 | 月报→季报 |
| 年报聚合 | 每年1月1日 00:03 | 月报→年报 |
| 自动备份 | 2/8/14/20 点整 | 数据库备份 |
| 日志清理 | 每天 04:00 | 清理 7 天前临时文件 |

---

> 📝 本手册随系统版本更新，最新内容以代码仓库为准。
